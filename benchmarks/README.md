# Benchmarks

Every benchmark instance must record its license, provenance, generator version,
checksum, variable count, clause count, and known satisfiability status.

This metadata is required whether an instance is hand-authored, generated, or
obtained from an external corpus.
